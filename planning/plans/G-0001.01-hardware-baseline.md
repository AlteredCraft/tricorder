# G-0001.01. Reproduce the actual board's hardware baseline

Status: In progress
Goal: [G-0001 — a trustworthy live investigation](G-0001-trustworthy-live-investigation.md)
ADRs: [ADR-0003 — diagnostic foundation](../adrs/ADR-0003-tab5-diagnostic-foundation.md).
Date: 2026-09-20
Revision: Initial proposal; no hardware results.
Revision: 2026-09-20 — consolidate the existing shared evidence format here from the planning overview; checks, workload and thresholds are unchanged.

Revision: 2026-09-20 — move ADR-0001's candidate design and research into this spec; no hypothesis, workload or threshold changes.

## Proposal

### Hypothesis

A pinned native toolchain and explicit peripheral ownership can repeatedly initialize this Tab5's built-in capabilities, with observed inputs and outputs attributable to the configured hardware.

### Refuted if

Any required peripheral fails repeatably, two libraries compete for a shared resource, documented identity does not match detected hardware without a supported driver, or a clean rebuild cannot reproduce initialization. An unexplained blank display or absent sensor is a failure to investigate, not a reason to silently remove that capability.

### Build

1. Inventory board/display labels, P4 silicon revision, camera identity, flash/PSRAM and C6 firmware. Treat the supplied ST7121 screen label as a clue until confirmed.
2. Reproduce the factory demo using its documented toolchain and capture resolved component revisions. Keep a known recovery image and its flashing instructions.
3. Build a small native diagnostic with one owner for each I²C/I²S/display/camera resource. Exercise touch, camera, microphones, speaker/headphones, BMI270, RTC, INA226, SD and Wi-Fi individually.
4. Compare the official BSP with M5Unified/factory drivers for unsupported functions. Record which source supplies each capability, including IO-expander-controlled power and reset behavior.

### Out of scope

This baseline tests the individual built-in functions. It does not establish concurrent performance (.02), quantitative microphone calibration (.03), agent usefulness (.04), or battery life/wake reliability (.05). USB host/device and RS-485 functionality need suitable peripherals/fixtures and remain deferred if unavailable; do not equate enumeration of a controller with a working application.

### How we know

Use the [shared evidence contract](#shared-run-evidence). Numeric limits are proposed acceptance targets, not verified performance.

1. Inventory: manifest.json contains actual identities, detection method and driver source. The capability matrix records verified / failed / not tested with evidence paths. Investigate the camera-name discrepancy in the BSP documentation against an ID read where supported.
2. Reproduction: archive build/flash/boot logs from two clean builds using the same pinned inputs. Explain any nondeterministic binary differences. Complete 10 cold starts and 10 software resets with zero initialization failures for the selected core capabilities.
3. Diagnostics: retain camera frames; verify captured test sound and playback via recorded observation; touch events cover corners/center; IMU logs respond to known orientations; RTC timestamps advance; SD test-file checksum matches after readback; Wi-Fi completes a logged round trip. Record audio physical-channel mapping rather than assuming stereo.
4. Driver selection: capability records identify resource owners and missing coverage. No unexplained duplicate initialization. Every built-in sensing path is tested or explicitly unresolved; core camera/audio/motion/touch/storage/network failures block .02.

Cross-run comparison: join summaries by spec revision and workload, retain all runs including failures, and compare at least three repetitions before claiming stable behavior. Differences in hardware, gain, clock, driver, model, codec or workload remain explicit.

### Shared run evidence

Each experiment should produce a private/local run folder, not committed personal captures:

- `manifest.json`: run/spec ID; UTC date and monotonic clock definition; board, silicon, display, camera and C6 firmware identity; firmware commit; toolchain/component versions and lock hashes; configuration; fixture/setup; expected workload; calibration status.
- `events.jsonl`: run/session/boot IDs, event name, device monotonic timestamp, host receipt timestamp when applicable, sequence number, capture ID and relevant fields.
- `metrics.csv`: timestamped workload, frame/capture counts, rates, latency, queue depth, overruns, allocation failures, free internal RAM/PSRAM, largest free blocks, task stack high-water marks, reset reason and power readings.
- `captures/`: test PCM/WAV, images and checksums with rate, gain, format, channels, processing flags, and sample/frame counters.
- `summary.json`: each acceptance check, sample count, p50/p95/max where appropriate, missing-data count, pass/fail/inconclusive, and paths to source records.
- `operator-notes.md`: dated observations, fixture movements and usability ratings, explicitly distinguished from instrument measurements.

These are proposed record formats to implement in the spikes. They do not yet exist. Every build step gets its check before implementation; append results under that spec's Observed section. Missing evidence is inconclusive, not a pass.

Use device monotonic time for local durations; never subtract unsynchronized host and device timestamps. Record boot boundaries. Use round-trip timing or a measured clock-offset estimate for cross-device analysis, including uncertainty. A display flush callback measures software completion, not necessarily photon-visible response.

Keep sampling and calibration settings comparable for A/B runs. Change one independent variable at a time before combining loads. Retain at least three repeated runs for a claimed performance result. Do not use appearance of the UI as proof of correctness; record physical observations and timing in run artifacts.


### Candidate native stack

Research and alternatives migrated from the former ADR-0001 on 2026-09-20. That file was a proposal, never a decision in force. The material below supports this experiment and its follow-on specs; no hardware suitability is established.

#### Context

The [vision](../../vision.md) calls for realtime handheld investigations and learning the entire device. Camera, audio acquisition, signal processing, motion, touch, playback and network activity must share resources predictably. Support for the processor alone is insufficient evidence of support for the complete Tab5.

Research snapshot: 2026-09-20. Primary documentation/source was inspected; no build or device validation was performed. Exact dependency versions remain undecided.

#### Alternatives considered

| Option | Verified basis | Main benefit | Main cost or unresolved question |
| --- | --- | --- | --- |
| Native ESP-IDF with C/C++ | Factory demo uses ESP-IDF; official Tab5 BSP exists [1][2]. | Direct control over tasks, memory, multimedia APIs and accelerators. | Embedded concurrency, buffer lifetime and board integration are our responsibility. |
| Arduino with M5Unified/M5GFX | Official Tab5 guide documents setup and peripheral examples [3]. | Quick hardware experiments and convenient APIs. | Advanced pipelines still require native APIs; Arduino defaults may not fit our workload. |
| Arduino as an ESP-IDF component | Official supported integration [4]. | Access an Arduino-only library inside a native project. | Additional configuration and version coupling. |
| UIFlow2/MicroPython | Official Tab5 workflow exists [5]. | Interactive development and familiar language. | Complete concurrent camera/DSP/speech/UI behavior was not verified. This is an evidence gap, not a claim that it is impossible. |
| Rust | Current esp-hal lists P4 support [6]. | Memory-safety tools and low-level control. | Complete Tab5 multimedia integration was not verified; current manifest labels P4 support for silicon revision 3.x or later [7]. |

M5Unified supports ESP-IDF directly [8]. We do not need to adopt Arduino simply to reuse its hardware abstractions.

#### Starting experiment direction

**Propose** C++ application code on ESP-IDF/FreeRTOS, LVGL 9, and selected vendor drivers behind a small board interface. Use C libraries where they are the supported path. Keep the native build system and component dependency records reproducible. This is the agreed starting experiment direction; record architectural choices in ADRs only when the linked experiments supply evidence for choices actually made.

| Layer | Candidate | Intended use |
| --- | --- | --- |
| Scheduling | ESP-IDF FreeRTOS [9] | Independent acquisition, processing, UI and transport tasks with bounded queues. |
| Interface | LVGL 9 and compatible display/touch integration [10] | Explore/Investigate/Compare views, graphs and capability diagram. |
| Board integration | Espressif Tab5 BSP, M5Unified and factory-demo references [1][2][8] | Select one owner per peripheral and shared bus; do not independently initialize the same device from competing stacks. |
| Audio I/O | esp_codec_dev and I²S [2] | Capture and playback, with explicit channel mapping and buffer ownership. |
| Signal processing | ESP-DSP [11] | Local FFT, filtering and quantitative comparisons. |
| Video | esp_video and camera drivers [12] | Capture, preview and supported sensor controls. |
| Acceleration | PPA, JPEG/H.264 where useful [12][13] | Image transforms and encoding after profiling. |
| Speech | ESP-SR [14] | Optional voice activity detection, AEC, noise suppression and bounded commands. |
| Local inference | ESP-DL [15] | Later task-specific models within measured resource budgets. |
| Wireless | ESP-Hosted / esp_wifi_remote with compatible C6 firmware [16] | P4-to-C6 networking over the board's SDIO connection. |

The factory demo documents ESP-IDF 5.4.2 and pins LVGL 9.2.2 [1][17]. Those are reproduction references, not an assertion that they are the best final project versions. Reproduce a supported baseline on this unit, then deliberately update and pin the selected set. The demo's desktop build is useful for UI work but cannot validate device timing, memory bandwidth, power or peripherals.

#### Costs and unresolved questions

We gain a direct route to native hardware features while learning scheduling, DMA/buffer ownership, memory placement and failure recovery. We take on C++ resource management, C-library integration, and driver compatibility work.

Specific assumptions to resolve:

- The supplied demo photo labels its LCD ST7121. Confirm the unit's label and active driver; M5Stack documents multiple panel revisions [18].
- BSP coverage is not complete: the published capability table marks battery support unavailable, despite the physical power-monitoring hardware [2]. Audit RTC, power telemetry, charging and wake separately.
- The BSP camera table and board description use different sensor names [2][18]. Identify the actual sensor and supported driver/mode instead of inferring it from a table.
- Native PPA or codec availability does not establish a frame-rate guarantee or prove that LVGL routes every drawing operation through hardware.
- The C6 firmware and host components are a compatibility set.
- Use speech processing without changing measurement audio; see [the audio-integrity experiment](G-0001.03-audio-integrity.md).

Evidence for adopting the candidate architecture comes from [hardware baseline](G-0001.01-hardware-baseline.md), [combined load](G-0001.02-concurrent-workload.md), and [audio integrity](G-0001.03-audio-integrity.md). Revisit if required peripherals cannot coexist, panel support remains unreliable, or another route substantially reduces measured integration cost.


#### Architecture follow-up

After each relevant .01–.03 result, assess whether the evidence supports an actual architectural choice or calls for another experiment. Record a bounded decision when made, linking the dated observations; do not wait for every optional capability or a complete milestone. Missing support remains a question in the spec. Optional ESP-DL, later acceleration and other deferred capabilities are not prerequisites for deciding the core stack.

#### Research references

These links support the research snapshot. Moving branches and “latest” pages can change; archive the selected revisions in each build's manifest.

1. [M5Tab5 factory demo, including desktop and IDF builds](https://github.com/m5stack/M5Tab5-UserDemo)
2. [Published Tab5 BSP 1.3.0 capability table](https://components.espressif.com/components/espressif/m5stack_tab5/versions/1.3.0/readme)
3. [Tab5 Arduino guide and driver compatibility](https://docs.m5stack.com/en/arduino/m5tab5/program)
4. [Arduino as an ESP-IDF component](https://docs.espressif.com/projects/arduino-esp32/en/latest/esp-idf_component.html)
5. [Tab5 UIFlow2 guide](https://docs.m5stack.com/en/uiflow2/Tab5/program)
6. [esp-hal support statement](https://github.com/esp-rs/esp-hal)
7. [esp-hal manifest and P4 silicon-revision note](https://github.com/esp-rs/esp-hal/blob/main/esp-hal/Cargo.toml)
8. [M5Unified supported frameworks and devices](https://github.com/m5stack/M5Unified)
9. [ESP-IDF FreeRTOS](https://docs.espressif.com/projects/esp-idf/en/stable/esp32p4/api-reference/system/freertos.html)
10. [ESP LVGL port 2.9.0](https://components.espressif.com/components/espressif/esp_lvgl_port/versions/2.9.0/readme)
11. [ESP-DSP 1.8.2, including P4-optimized routines](https://components.espressif.com/components/espressif/esp-dsp/versions/1.8.2/readme)
12. [esp_video 2.5.0 support matrix](https://components.espressif.com/components/espressif/esp_video/versions/2.5.0/readme)
13. [P4 Pixel-Processing Accelerator](https://docs.espressif.com/projects/esp-idf/en/stable/esp32p4/api-reference/peripherals/ppa.html)
14. [ESP-SR](https://github.com/espressif/esp-sr)
15. [ESP-DL](https://github.com/espressif/esp-dl)
16. [Factory-demo hosted-network dependencies](https://github.com/m5stack/M5Tab5-UserDemo/blob/main/platforms/tab5/main/idf_component.yml)
17. [Factory-demo library versions](https://github.com/m5stack/M5Tab5-UserDemo/blob/main/repos.json)
18. [Tab5 hardware, pin map and revisions](https://docs.m5stack.com/en/core/Tab5)

## Observed

2026-09-20: **Not built, design only.** No build steps or device tests have been performed for this spec. All proposed checks and all out-of-scope items are **Not tested**. There are no measured outcomes or accepted design changes. Future entries must name the build step, dated result and evidence path, and link an ADR if evidence changes the design.

2026-09-20: **Not tested — planning consolidation.** This spec now owns the shared run evidence format linked by the other G-0001 specs. No checks or thresholds changed and no build ran. See the existing stack decision in [ADR-0001](#candidate-native-stack); no new architecture decision was made.

2026-09-20: **Not tested — ADR reconciliation.** The earlier ADR-0001 reference identifies a former proposal, not an architectural decision in force. Its research and alternatives now live in the candidate-design section above. The original file is retained in Git history; no decision was revoked and no device result was added. Architecture disposition: unresolved candidate choices; next action is the experiment described here, followed by evidence-based decision recording where appropriate.

2026-09-20: **Steps 1–2 started.** USB serial opened at `/dev/cu.usbmodem1101`; exact chip/peripheral identities remain pending. Six test-first recovery checks pass (matching complete reads, size/hash/blank/missing-data failures, append-only manifest). Factory source pinned to `b4e356b`; IDF 5.4.2 setup underway. Private evidence: `.local/runs/20260920-baseline/`. Next: identify silicon, verify two complete flash reads, reproduce the demo. No architecture decision yet.

2026-09-20: **Steps 1–2, recovery/build evidence.** Correct ROM port is `/dev/cu.usbmodem4`: P4 v1.3, 16 MB flash, secure boot/encryption disabled. Two complete reads match SHA-256 `a260eda9…fde231`; original app identifies as factory `639d9cc`, IDF 5.4.2. Two clean builds of pinned factory `b4e356b` succeed; differences are compile timestamps and derived hashes/checksum only. Evidence: `.local/runs/20260920-baseline/{recovery.json,original-image-info.log,build-comparison.json}`. Original passive boot capture is empty, not a boot pass. Next: flash/boot reproduced demo, then instrument native diagnostics. No architecture adopted yet.

2026-09-20: **Step 3 checks written before implementation.** Native diagnostic must emit boot-scoped sequence/timestamps; record sensor IDs, RTC advancement and SD checksum readback explicitly. Missing check records remain inconclusive, sequence gaps/failures survive aggregation, and source-level startup messages never count as physical validation (14 host checks pass). Preserve touch/IMU observations for operator fixtures. Camera/audio/network acceptance remains separate and pending until captures/round trips exist.

2026-09-20: **Operator fixture:** user confirms no microSD installed. SD readback and SD-backed capture/recovery checks remain pending a card; this does not establish a storage failure or pass.

2026-09-20: **Step 3 failed run retained.** Native identity reads match BMI270 `0x24`, INA226 manufacturer `0x5449`, and camera PID `0xeb52` (factory SC202CS driver; marketing-name discrepancy retained). `.local/runs/20260920-baseline/diagnostic-boot-1/` records a repeatable PPA alignment abort with the BSP default display buffers. Next: use the already boot-tested factory full-frame PSRAM configuration and require a sustained run without restart. RTC/SD were not reached; no functional camera claim.

2026-09-20: **Steps 2–3 partial success.** Both clean factory builds now have verified flash/startup logs. Corrected native run `diagnostic-boot-2` records 34.4 s, one boot, no event gaps/capture errors, matching IMU/INA226/camera IDs and advancing RTC; SD remains inconclusive (no card). ST7121 and 32 MB PSRAM are detected. Recovery instructions are in private `.local/runs/20260920-baseline/RECOVERY.md`. Adopted the bounded diagnostic foundation in [ADR-0003](../adrs/ADR-0003-tab5-diagnostic-foundation.md); functional camera/audio/network, physical fixtures and repeatability gates remain open.

2026-09-20: **Physical input observed.** User confirms stable diagnostic screen. `diagnostic-input-1/events.jsonl` records all four corner regions plus center and accelerometer changes during requested tilts. Private `capabilities.json` distinguishes this limited verification from pending camera/audio/network/SD and restart gates. Next: functional media and LAN diagnostics; microSD-dependent checks await a card.

2026-09-20: **Checkpoint.** Three-minute physical-input capture completed with one boot and no event gaps or capture errors; five target regions and tilt response retained. Sixteen host tests and bootstrap rerun pass. Full .01 acceptance remains open; details and failed attempts are retained in local evidence and implementation history.
