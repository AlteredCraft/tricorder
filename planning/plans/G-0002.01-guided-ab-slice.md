# G-0002.01. Complete one guided A/B loop on the handheld

Status: In progress — transport hardening and OpenRouter live work resumed; physical/live acceptance incomplete
Goal: [G-0002 — one usable end-to-end investigation](G-0002-thin-investigation-slice.md)
ADRs: Existing [foundation](../adrs/ADR-0003-tab5-diagnostic-foundation.md), [capture completion](../adrs/ADR-0004-capture-completion.md), [radio lifecycle](../adrs/ADR-0005-diagnostic-radio-startup.md), [raw/speech ownership](../adrs/ADR-0006-raw-audio-and-derived-speech.md), [camera FIFO](../adrs/ADR-0007-bounded-camera-fifo.md), [JPEG fail-stop](../adrs/ADR-0008-jpeg-error-path-fail-stop.md), and [preview ownership](../adrs/ADR-0009-owned-preview-and-render-placement.md) constrain reused components. The bounded device/service mock path is now recorded in [ADR-0010](../adrs/ADR-0010-device-owned-ab-lan-experiment.md); [ADR-0011](../adrs/ADR-0011-bounded-tcp-write-completion.md) adds bounded TCP completion and [ADR-0012](../adrs/ADR-0012-live-prose-over-verified-summaries.md) adopts live prose over verified summaries. Speech architecture remains open.
Date: 2026-09-21
Revision: Initial proposal; narrower early usability experiment, not a revision of any G-0001 acceptance gate.

## Proposal

### Hypothesis

A single explicit-turn A/B sound investigation on the Tab5, guided through the Mac LAN service first by a scripted mock and then by a live provider, can produce a useful evidence-grounded comparison without requiring the operator to use the Mac during the loop. Existing instrument code can support this product path and subsequently be hardened in place.

### Refuted if

The operator cannot complete the loop without developer intervention; guidance cites the wrong or absent capture; changed settings make the comparison misleading; network waits prevent local cancel; or the operator cannot understand the result or identify a useful next action. Missing captures, transcripts or operator observations are inconclusive, not passes. A reproducible defect requires repair and another retained run, not a waiver.

### Build

Write the corresponding checks below before each implementation step. Inspect current source and Active ADRs before changing ownership or scheduling.

1. Define one reproducible fixture: for example, ask how the recorded sound changes between two distances from a stationary fan. Record device placement, gain, physical audio slot, sample rate and capture duration; change only the stated A/B variable. Do not claim calibrated sound pressure or infer an object's motion from an uncoupled handheld IMU. Freeze the exact fixture and expected comparison before the first run.
2. Implement the smallest complete on-device flow: explicit ask/turn control, capture A, evidence-linked guidance, operator adjustment, capture B, and an inspectable comparison. Show recording, waiting, offline, cancelled and incomplete states. Accept spoken input and provide spoken guidance with visible text; a preset question/text-only path may bootstrap the mock, but does not replace the live handheld loop. Setup may use the Mac; no keyboard intervention during an acceptance loop.
3. Build the Mac Python mock service and real device transport. Reuse the [candidate service architecture](G-0001.04-agent-interaction.md#candidate-service-architecture): device-initiated configurable LAN endpoint, bounded typed commands, boot/session/request/capture identity, deadlines, acknowledgements, cancel and duplicate/stale rejection. The mock must consume actual captures and deterministic measurements, never invent substitute evidence. Keep only the streams needed for this fixture active; record the actual overlap/mode policy rather than claiming full concurrency.
4. Replace only the provider adapter with live model/speech services. Keep keys on the Mac, sanitize logs, and retain the same flow and evidence contract. Provider choice and credential setup require the user; mock development can proceed without them. Start turn-based; label speaker-active intervals and avoid including device playback in A/B measurement windows. Continuous conversation/full-duplex is not implied.
5. Observe the handheld runs, fix blocking defects, and review usefulness with the user. Retain the working implementation, list its measured limits, and map outstanding hardening work back to G-0001. Do not expand this slice into a full sensor dashboard or complete every diagnostic before demonstrating the loop.

### Out of scope

Full Explore/Investigate/Compare breadth, all simultaneous sensors, sustained streaming, calibrated acoustics, hosted deployment and full-duplex speech. The original [concurrency](G-0001.02-concurrent-workload.md), [audio](G-0001.03-audio-integrity.md), [interaction](G-0001.04-agent-interaction.md), [hardware/storage](G-0001.01-hardware-baseline.md) and [power/recovery](G-0001.05-power-recovery.md) gates remain required for G-0001. The slice may use bounded temporary device retention and Mac evidence storage; this does not pass SD or interrupted-write recovery checks.

### How we know

Use the [shared evidence contract](G-0001.01-hardware-baseline.md#shared-run-evidence), with explicit G-0002.01 spec/revision and workload metadata. Preserve failures and private captures; incomplete exports cannot support a conclusion. These are proposed slice checks, not replacements for G-0001's numerical gates.

1. Before the UI/transport build, host tests cover the complete state sequence and reject wrong-session, unknown-capture, expired and duplicate commands. Inject a delayed response, cancellation and disconnection; no stale command may trigger a capture or overwrite the current result. Queue/storage limits must be explicit and tested at capacity. Mock/provider adapters share contract tests.
2. Complete three mock handheld A/B loops. Each must retain the question, two distinct real capture IDs with source settings and hashes, guidance, the recorded operator adjustment and a comparison that joins to those exact captures. Independently check the deterministic measurement values used in the comparison. No fabricated or cross-session evidence, hidden developer intervention, crash or unexplained measurement loss is allowed.
3. In separate controlled mock trials, delay a response by five seconds, cancel a pending turn, and disconnect/reconnect the service. Record local state transitions and their timing; waiting/offline states remain visible, local cancel remains operable, and old-session responses cannot act. This is basic flow validation, not .02's backpressure gate or .04's full cancellation/recovery statistics.
4. Complete three live-provider handheld loops using the same contract. Retain sanitized transcript/evidence joins and timestamps for capture, transfer, inference, first displayed/audible response and cancel where exercised. After each loop the operator gives 1–5 responsiveness/usefulness ratings (proposed target: at least 4 for each dimension in each run), explains the A/B finding, and identifies a next action without developer interpretation. Record latency distributions without claiming .04's 30-turn acceptance from three loops.
5. Close with a requirement-by-requirement result and architecture disposition. Identify reusable implementation/evidence and remaining G-0001 gates. G-0002 is not Met on mock results alone, missing live-provider access, a single polished demo, or unresolved usefulness/integrity failures. Broader reliability remains unproven even if all slice checks pass.

## Observed

2026-09-21: **Not built, design only.** User requested an early vertical-slice goal and explicit relationship to G-0001. No implementation or run evidence exists for this spec. No architectural decision is made by choosing an experiment sequence. Existing component ADRs remain constraints; service/protocol choices require evidence and decision records as implementation proceeds. Work remains paused until resumption is requested.

2026-09-21: **Implementation resumed; host-only checkpoint.** User requested work on G-0002 and selected a speaker playing a steady sound. Wrote failing checks before the state/evidence, mock service and collector changes, then implemented the reference A/B reducer, verified PCM measurements, shared reply validation, bounded capture/transcript retention and device-initiated WebSocket service endpoint. 156 host tests pass (including two real localhost WebSocket tests); retained log: `.local/runs/20260921-g0002-host/tests-deadline-final.txt`. Localhost tests exercise a synthetic complete A/B exchange and cancellation during a five-second delay; host tests cover stale/duplicate/wrong-session commands, corruption, timeouts, partial retention and full three-second capture capacity. [Protocol and prepared fixture](../guided-ab-protocol.md) describe exact bounds and remaining work. No firmware change/build/flash or physical run was performed; these tests do not establish device cancel timing, actual audio quality, LAN latency or usefulness. Fixture source level/room/placement details must be frozen before the first run. Provider access and speech integration remain absent. Architecture disposition: experimental protocol implementation; defer adoption decisions until real device evidence.


2026-09-21: **Device implementation and startup checkpoint.** Wrote failing native C++ protocol/framing checks and host ingress-proof checks before their implementations. Added the single-owner C++ reducer, bounded pinned-IDF WebSocket transport, raw audio capture/upload and manual A/B screen. 168 host tests pass, including ASan checks for capture failure/cleanup/cancel and both localhost tests (`.local/runs/20260921-g0002-device/tests-final.txt`). The P4 trial build passes and boots with display/radio initialization successful. Exact 71-file source/config/build snapshot and binary hash are in `trial-1-manifest.json` and `trial-1-artifacts/` under that directory. The optional guided-startup mode skips automatic diagnostic stages; those skips do not pass G-0001 gates. Current device boot is `ac14598193e5acc7f789ee69e496c351`, observed on the rediscovered identity-matched `/dev/cu.usbmodem101`. The user is available for the physical trial; fixture details, real mock loops and failure trials are pending. Speech/live provider and acceptance remain untested. Architecture disposition: existing ownership/completion ADRs remain in force; protocol/transport adoption awaits the device trial. See the [device checkpoint](../guided-ab-protocol.md#device-integration-checkpoint--2026-09-21) for bounds and setup.


2026-09-21: **First operator attempt blocked by setup UI; retained failure.** The Tab5 joined Wi-Fi, but the operator could not enter the endpoint because the keyboard was off-panel. Source inspection identifies LVGL's keyboard constructor setting bottom-center alignment; the later `set_pos(..., 15, 270)` retained that anchor and moved it below the visible panel. No WebSocket session or A/B capture occurred. Serial trial 1 ended without integrity/capture errors; its generic summary is inconclusive, not a slice pass. Added a device-side check of the resolved keyboard rectangle before correcting alignment to top-left. Added an optional editable endpoint build default to avoid retyping the experiment address after flashes. The user selected MacBook built-in speakers at 20% volume; a prepared 1000 Hz tone was played, but room/background and distance measurement method are not recorded, so the next run is explicitly developmental rather than one of the three acceptance loops. Source/config changes require a fresh archived build and trial. Independent saved-run assessment now has separate byte recomputation and transcript-join checks; 171 host tests pass (`tests-with-assessor.txt`).


2026-09-21: **Keyboard correction boots; fixture revised before capture.** Trial 2 boot `64858a093a4fcb96cf5c0c96e6a90c92` passes the resolved keyboard-bounds, display and radio checks; final serial summary passes those three named startup checks only. No session/capture occurred before the user requested 30% MacBook volume and imperial measurements going forward. Revised the prepared fixture to 8-inch/16-inch positions and made device prompts/adjustment text derive from that same embedded JSON. Preserve the old 20% source setup and trial artifacts as history; the next formative fixture is `speaker-setup-development-2.json`. Room/background and exact distance method remain unreported, so this is not yet a frozen acceptance fixture.


2026-09-21: **First real mock loop completed; measurement interpretation fails.** Session `ab-5340de189b6408c3fff7faf38ba94c6f`, boot `82e179b1574d63b6097bdd90e92fb08e`, completed ask/A/guidance/adjust/B/comparison with two 1,152,000-byte real captures, exact settings, complete ingress-block and driver proofs, and matching acknowledgements. The independent byte/transcript assessor passes (`development-1-assessment.json`), and the operator confirmed the comparison appeared, tone was continuous, and A/B positions/orientation were followed. However the displayed +14.100 dB B/A ratio is dominated by startup transients, not a useful steady-tone comparison: slot-0 RMS per first 100 ms is 401.65/2083.59 counts, versus about 13/5 after settling; both largest slot-0 samples occur at frame zero. These facts support a codec-startup hypothesis, not a calibrated acoustic finding. Preserve raw captures, `development-1-window-analysis.json`, the operator response and firmware artifacts; do not count this as an acceptance pass.

2026-09-21: **Settling correction built; repeat pending.** Added failing tests, then an explicit 24,000-frame (0.5-second) discarded settling prefix before the unchanged 144,000-frame raw measurement. RX stays continuous across that boundary. Metadata declares `warmup_frames` and `epoch_start_us`; full-epoch counters must cover warmup plus retained raw bytes, and block hashes cover every retained measurement frame. No old raw bytes are trimmed or replaced. The host and independent assessor verify the expanded epoch accounting. 172 tests pass (`tests-settling.txt`); P4 build/flash passes with exact `trial-4-artifacts/` and `trial-4-manifest.json`. Added deferred cancel-request/owner-stop timestamps without a network/logging wait in the UI callback. This is a candidate correction requiring a real repeat; driver proof still does not guarantee acoustic stationarity. A service/protocol/measurement-boundary ADR will be assessed after repeat evidence.


2026-09-21: **Architecture disposition after the first real exchange.** [ADR-0010](../adrs/ADR-0010-device-owned-ab-lan-experiment.md) adopts the bounded device-owned mock state/evidence flow and current LAN transport as the shared implementation to harden. The successful byte/identity/ACK joins support that limited choice despite the independently retained acoustic-interpretation failure. This does not adopt a live provider, validate cancellation timing, or claim that the candidate settling interval is sufficient. Existing ADR-0003 through ADR-0009 remain applicable; none is revoked.


2026-09-21: **Settling-enabled real loop passes technical checks; live work deferred.** Boot `38b2902ae2856610cc8e82161f5090a4`, session `ab-d6c9eb239c7b6f26b81ed7127b33bf0f`, completes with two distinct real raw captures and passing independent source/ingress hashes, full-epoch counters and request/reply/ACK joins. Retained windows are 3,000,297/3,000,274 microseconds within 3,500,344/3,500,323 microsecond epochs. A/B slot-0 RMS is 20.9719/9.38288 counts (−6.9860 dB B/A), peaks 75/65, no clipped samples. First-100-ms RMS is 20.626/6.725 counts; the previous frame-zero spikes are absent. The operator reports the A/B test is done. See `development-2-assessment.json`, `development-2-window-analysis.json` and `development-2-operator.json`. This supports the bounded settling correction, not calibrated sound pressure, universal stationarity or full acceptance. An earlier new session (`ab-b02d396d1bbf3e0f139894bba8d2bfb0`) idled out at ready-A after 30 seconds and was replaced by the fresh successful session; retain it as an observed timeout/restart, not the planned controlled disconnect trial. Two repeat loops, fixture characterization and controlled delayed/cancel/disconnect trials remain pending. The user explicitly deferred live-provider work on 2026-09-21; resume that phase only when requested. Architecture disposition: retain ADR-0006's immutable measurement boundary and ADR-0010's device-owned mock path. The measured 0.5-second prefix is a versioned experiment setting, not a new general architecture choice; no further ADR or revocation is warranted by this bounded result.


2026-09-21: **Paused at the user’s request.** After confirming the corrected A/B loop, the user reported that someone started a lawnmower outside and asked to hold off. Stopped the mock listener and serial collector; tone playback is finished. Final serial trial-4 summary passes its three named startup checks with no sequence/capture errors; this does not pass the whole spec. Noise timing relative to the completed measurement is not established, and no quiet-room acceptance claim is made. Keep the currently flashed trial-4 image, 172-test result, both raw A/B pairs, the timeout session and all earlier failures. `pause.json` records the disposition. Resume physical work only when requested in stable background conditions: freeze placement/background details, complete remaining mock repetitions and controlled delay/cancel/disconnect checks. Live-provider work remains separately deferred. Goal/spec remain In progress and incomplete; no numerical gate is lowered.


2026-09-21: **Resumed; two additional developmental loops complete.** The user reported mowing stopped and confirmed both repeats. Same trial-4 image, new boot `37fdb4d143843711ca7025e0ac5ccbb7` after unintended USB-reattach reset. `mock-trial-3` sessions `ab-0f79af7554d4a7b17da1e633e9d69da7` and `ab-241560a8fd2bb20e39b7582b2dbc2219` independently pass byte/block/counter/setting/transcript joins, with B/A −7.5752/−9.9557 digital dB, zero clipping and no original startup spikes. Evidence: `development-3/4-{assessment,window-analysis,operator}.json` under the existing private run root. Together with the earlier corrected loop, three developmental mock loops complete; placement precision/background characterization remains unreported, so these do not retroactively satisfy the frozen-fixture requirement. Preserve the original serial observation-boot mismatch and assess actual new startup separately. Controlled delay/cancel/disconnect checks remain underway; live work remains deferred. Architecture disposition: retain ADR-0010 and versioned settling setting; no new architecture adoption or numeric-gate changes.


2026-09-21: **Operator clarification for the two resumed repeats.** The user reports “measured, no bg noise to speak of.” The 8-inch/16-inch positions were measured and no noticeable background noise was reported for sessions `ab-0f79af7554d4a7b17da1e633e9d69da7` and `ab-241560a8fd2bb20e39b7582b2dbc2219`. Measuring tool and handling noise were not separately characterized. `repeat-fixture-clarification.json` supplements the original private operator records. This is a post-run clarification limited to those two repeats, not a pre-run fixture freeze or a statement about the earlier corrected loop. No acceptance gate or architecture disposition changes.


2026-09-21: **Five-second delay and cancellation observed; local timing incomplete.** In `mock-delay-1`, session `ab-0b9d7d3761f67fe862aa843acc902f23` completes with host response delays 5.0201/5.0298 seconds and independent raw/transcript checks passing. The tone had ended, so this is a control-flow trial, not another speaker-distance acoustic result. A separate session `ab-91e91c781c86ab1f149f57004ef59159` uploads A, sends a turn, then cancels while pending: host records cancel/cancelled/closed, with no subsequent guidance/comparison. The operator reports Cancel responded promptly. Device cancel timing is unobserved: serial-trial-5 reached its 1,200-second bound before those transitions. Preserve its ordinary FAIL summary (unexpected declared observation boot and malformed first line caused by a partial old telemetry line joined to the new ROM boot banner). A separate assessment of parsed new-boot events passes startup checks and sequence continuity only; it does not repair raw-log errors or missing later timing. `delay-cancel-1-assessment.json` retains the scoped result. Reopen serial with a 3,600-second bound for remaining control checks; no complete failure-trial acceptance claim yet. Live work remains deferred.


2026-09-21: **Instrumented capture cancellation.** Boot `9b7157204331e6b81fceb8b95806e446`, session `ab-dcef3420a6b6f5d31aa214682ad77afd`, records ready-A → recording-A → cancelled. Local cancel request 492,413,491 us; capture-owner stop 492,429,412 us: 15.921 ms. This is request-to-owner-stop timing, not touch-to-panel or playback timing. Cancellation happened during recording, about one second after recording began, not while awaiting a response. The host records hello/ready/cancel/cancelled/closed, with no capture uploaded. Operator reports done. `capture-cancel-1-assessment.json` retains the scoped result. Earlier pending-turn cancellation remains supported by host evidence/operator observation without device timing. Controlled disconnect/reconnect is now armed through private `disconnect_fixture.py`: close the existing mock one second after its first turn, remain unavailable five seconds, then restart at the same endpoint. This uses the existing server unchanged. Collector 82141 stays open; fixture handle 2014. Await operator action and observed terminal/recovery states; no recovery pass claimed yet.


2026-09-21: **Controlled disconnect observed; recovery pending.** Session `ab-57f91c65ab75b51c5d84b8be0493f53b` uploads A, enters Waiting, then Offline after the service closes during its delayed response. The operator confirms “Offline. Check Wi-Fi and Mac Service ...”. Waiting-to-Offline device state interval is 1110.363 ms; this includes the intentional one-second pre-close wait and is not disconnect-to-panel latency. The host sends no guidance/comparison, closes the old session offline, and restarts after 5.002647 seconds unavailable. Saved capture SHA-256 matches. No later state/action appears for the old session in the observed log. `disconnect-1-assessment.json` retains results. A fresh Start mock → record A → guidance → Cancel is requested to establish usable recovery; no recovery pass claimed until observed. Serial collector 82141 and recovery fixture 2014 remain active; no tone is running.


2026-09-21: **Fresh-session handshake and ready-state cancel verified; post-capture recovery still pending.** The operator reports guidance appeared and Cancel worked. Session `ab-0f9749920ccfa8aa553b1da00ea37bbf` has hello/ready/cancel/cancelled/closed and device ready-A → cancelled, with request-to-owner-stop 12.454 ms. It contains no recording, turn or evidence-linked guidance: the displayed initial fixture prompt cannot establish post-capture recovery. Preserve both the operator report and narrower instrumented result in `reconnect-1-ready-cancel-assessment.json`. Requested explicit Start mock → separate Record A → wait for Confirm position B → Cancel. Service fixture 2014 and serial collector 82141 remain running; do not reset/reopen USB. Live-provider work remains deferred.


2026-09-21: **Fresh recording/guidance recovery verified; current mock trial session closed.** New session `ab-0b7963eda0072dfdbba664eba30f25a8`, same boot `9b7157204331e6b81fceb8b95806e446`, records ready-A → recording-A → uploading → waiting → adjust → cancelled. The operator confirms Confirm position B appeared and Cancel worked. Independent recomputation verifies the 1,152,000-byte recording and every ingress block, declared settling/epoch counters, source settings, computed guidance measurement and exact request/capture/ACK joins. The interrupted session stays Offline with no later actions. Cancel request-to-owner-stop at adjust is 0.485 ms, not touch-to-panel/playback timing. `reconnect-2-assessment.json` retains this bounded recovery pass; the prior handshake-only attempt remains separately recorded.

Serial trial 6's ordinary summary remains FAIL for one malformed first attachment line: buffered partial old instrumentation joined to the new ROM banner. All subsequent parsed current-boot events have continuous sequence/time, and the three startup checks pass; `parsed-current-boot-assessment.json` explicitly preserves that limited scope. Do not replace the original summary. Device timing during the earlier five-second pending-turn cancellation remains unobserved because serial trial 5 had ended; no aggregate cancellation/latency gate is claimed. Service fixture 2014 and collector 82141 are terminal, tone finished, all 72 trial-4 source/build hashes still match. Same trial-4 firmware stays flashed. No commits/push; live-provider work remains deferred by the user. `final-checkpoint.json` records closeout. Architecture disposition: retain ADR-0010's bounded mock path; this single recovery result does not justify a broader transport/reliability decision.

### Requirement disposition at the 2026-09-21 mock checkpoint (historical)

| Slice check | Recorded result and remaining scope |
| --- | --- |
| Host protocol/state/limits | 172 tests pass for the current checkpoint, including native ASan checks and local WebSocket tests; device evidence adds real source/ACK joins. |
| Three mock loops | Three corrected developmental A/B loops pass independent evidence checks. The two resumed repeats have measured 8/16-inch placement and no noticeable background noise reported afterward; the first corrected loop's full fixture notes are incomplete. Preserve the pre-freeze limitation and original transient failure. |
| Delay/cancel/disconnect | Five-second replies observed; pending cancellation suppresses reply; capture cancellation has 15.921-ms owner-stop timing; controlled service disconnect and fresh recording/guidance recovery verified. Pending-turn device timing and actual panel latency remain unmeasured; isolated control runs do not pass G-0001's statistical gates. |
| Three live/speech loops and usefulness | Deferred by explicit user instruction; not implemented or accepted. |
| Closeout and architecture | Evidence and limits recorded, ADR-0010 retained, all trial processes stopped. G-0002/G-0001/M-0001 remain incomplete; no thresholds waived. |


### 2026-09-22 — SD-assisted development checkpoint

The newly supplied SD card is mounted through the existing BSP without formatting.
The requested local Northbank environment file was provisioned over USB; settings
persist across reset and the Tab5 joins the LAN. Added checked SD A/B raw/metadata
archives before upload, public-only LAN export, and independent host download
validation. The existing media owner and local-capture controls remain in charge.

175 host tests and the P4 build pass. Real startup SD/radio/HTTP checks pass;
five 1,152,000-byte synthetic files download with matching hashes, all three
isolated LAN challenges pass, and five forbidden download paths return 404.
Write/readback takes about 3.01 seconds per raw file. Synthetic storage files are
explicitly labeled and do not count as new A/B loops. Retained initial association,
serial-attachment and concurrent-download/echo failures are indexed in HANDOFF.md.
Exact artifacts and evidence: `.local/runs/20260922-sd-testing/`.

One real SD-backed A/B loop and physical interrupted-write recovery remain open.
Live-provider/speech work stays deferred. No existing acceptance criterion or
failed evidence has been replaced by this development checkpoint.


### 2026-09-22 — first real SD A/B trial exposed DMA exhaustion

The operator attempted A/B with approximate 8/16-inch positions, MacBook speakers
at 30%, and the retained 1000 Hz source. A uploaded and received guidance; after
recording B the device asserted in hosted `sdio_rx_get_buffer` when its aligned
DMA allocation failed, then rebooted. The operator confirmed a restart and no
comparison. Serial and protocol assessments remain FAIL/incomplete.

Both A and B nevertheless committed to SD before the reset. Independent download
checks pass for 1,152,000 bytes and 141 ingress proofs each. A's SD raw bytes and
original metadata exactly match the Mac upload; B never uploaded. Offline raw
RMS difference is -0.04259 digital dB, with no clipping. Distances were approximate
and noise was not reported; do not infer a calibrated or useful distance effect.
This is SD recovery evidence, not a completed live A/B comparison or physical
power-interruption acceptance. Evidence: `.local/runs/20260922-sd-ab-1/`, especially
`serial/`, `ab-assessment.json`, `sd-recovery-assessment.json`, and `operator.json`.

Inspected cause: the reducer deep-copied each full ingress tree although it uses
only capture ID/end time; the regression fixture measures 62,643 allocated bytes
for one snapshot. Small/default allocations and 16-KiB SD scratch competed with
hosted radio DMA. The correction retains only owned ID/end-time and measurements,
uses explicit PSRAM for SD/HTTP scratch, and serializes metadata after raw I/O.
Regressions require snapshot allocations below 2 KiB, PSRAM scratch selection and
clean failure on scratch-allocation exhaustion. 176 host tests and P4 build pass.
`memory-fix-manifest.json` archives the candidate; real A/B revalidation remains
required. New archive resource events report DMA free/largest blocks and the
since-boot low-water mark; checkpoints are not continuous profiling.


### 2026-09-22 — retry survived; SD replay completes without new acquisition

The memory-fixed physical retry did not restart. Both raw/metadata pairs saved
and independently verify, but B's upload failed before the mock received its
metadata. User reported the device showed “Incomplete.” Original boot
`b936e25692766aba3758affc26b1bd55`, session
`ab-2f45739e04fe71cdd5302938fec1a3b7`; evidence under
`.local/runs/20260922-sd-ab-2/`. Keep its incomplete protocol assessment and ordinary
serial FAIL (one malformed line). The downloader's unrelated old synthetic-file
HTTP failure also remains; the scoped real-pair recovery assessment passes.
No reboot, SD write failure or radio assertion was observed in this retry.

Added tests before an explicit USB-only SD replay path through the existing media
owner and A/B protocol. It loads one immutable capture at a time, verifies raw
extent/hash and recomputes measurements; no codec opens or new timestamps are
invented. Normal service mode rejects the replay flag; replay-only mode requires
it. Manifest, UI and independent assessment label the replay. Original failed
runs are not overwritten or promoted. Numeric send diagnostics and host close
codes will help investigate the intermittent upload failure.

Three hardware replays complete with both received captures and original metadata
matching SD byte for byte. Replay 2 deliberately slows host chunk processing by
45 ms; replay 3 exercises the reusable collector option. Evidence directories:
`20260922-sd-replay-1`, `-2`, `-3`. All independent A/B assessments pass; only replay
1's ordinary serial summary passes, while 2/3 preserve malformed attachment-line
FAILs. The physical retry's exact transport cause remains unconfirmed; these
replays did not reproduce it and no transport deadline was relaxed. Replay is
recovery/diagnostic evidence, not another physical loop or a connection fix.

Recorded B/A digital RMS is +0.9380 dB, contrary to the distance hypothesis,
without clipping. Phone-gauge instructions were supplied (about 7.9/15.7 inches),
but final placement/noise and source audibility are not independently established.
Do not infer acoustic acceptance. 180 host tests pass, P4 build/flash passes;
`20260922-sd-ab-2/final-manifest.json` identifies the final source/tool checkpoint
and the same replay diagnostic image. Tone and test services are stopped. Live
provider/speech and original acceptance gates remain deferred/open as before.

2026-09-22: **Four-stage continuation authorized.** User requested transport diagnosis, a fresh controlled physical loop, live-provider/speech integration, then the original G-0001 reliability gates, and selected OpenAI. This supersedes the earlier live-provider deferral; no acceptance criterion is waived. Before the transport change, added a failing native regression for positive short TCP writes, fixed-budget exhaustion, no progress, errors and cancellation. Inspection of pinned `transport_ws.c::_ws_write` and `transport_ssl.c::tcp_write` shows single-call payload transmission can return a positive short count. The candidate completes remaining bytes below WebSocket framing without resetting the timeout, and invalidates a failed connection. This is a demonstrated code weakness, not yet the proven cause of the historical B failure: that original run lacks the numeric send diagnostic. New evidence is retained under `.local/runs/20260922-transport-resume/`. Physical replay needs reachable Wi-Fi; the verified USB board is connected, but the saved access point is unavailable on the current Mac LAN. OpenAI credentials are requested by local path. Existing ADRs remain in force; transport/provider changes require further validation.

2026-09-22: **Transport candidate and approved live text probe.** Native regression passes bounded positive short-write completion, expiry, cancellation and error cases. P4 build and flash pass, and normal SD replay on the candidate independently passes bytes/transcript joins (`candidate/assessment.json`) with a clean serial summary. The candidate preserves the pinned TCP socket getter and shared error foundation required by WebSocket close handling. An 80 ms/chunk receiver fixture produces an explicit two-second write timeout and Incomplete; this is retained, not presented as a completed slow transfer. Neither result proves the exact cause of the historical B failure, which lacks send-count diagnostics. User supplied a new local Wi-Fi configuration; provisioning and three LAN echoes pass after retained initial failures. User selected OpenRouter `openai/gpt-5.6-sol` and explicitly approved sending two saved evidence summaries. Both actual model calls pass the unchanged host-owned measurement/reference contract in 3.21/2.93 seconds (`openrouter-smoke.json`). No raw audio or Wi-Fi settings were sent. 192 host tests pass (`tests-3.txt`). Evidence paths are relative to `.local/runs/20260922-transport-resume/`. A fresh physical trial is active with one serial owner; its first session uploaded A, then timed out at adjustment during an accidental conversation interruption. A new session is requested. Speech, usefulness, physical-loop completion and original G-0001 gates remain open. Architecture disposition: [ADR-0011](../adrs/ADR-0011-bounded-tcp-write-completion.md) adopts bounded TCP completion and [ADR-0012](../adrs/ADR-0012-live-prose-over-verified-summaries.md) adopts live prose over verified summaries. Earlier ADRs remain applicable; no speech architecture is yet adopted.


2026-09-22: **Fresh physical A/B completed; local commit checkpoint.** Boot `92ca13cf587e052c0a184f6033f42c4e`, session `ab-37d8b855eaf9b6a50662cc17bcd1f9ad`, completes A → guidance/ACK → B → comparison/ACK. Operator reports “Comparison appeared.” Independent assessment verifies both raw files/hashes, ingress proofs and transcript joins; B/A digital RMS is −3.889417605054226 dB, with zero clipped samples. Before recording, the operator confirmed audible MacBook built-in speakers at the prepared 30%/1000 Hz setup, quiet room and measured placements; the exact ruler/tape remains unspecified. Retain that fixture limitation. `physical/assessment-1.json`, `physical/operator-1.json` and `physical/fixture.json` record the result under `.local/runs/20260922-transport-resume/`. This is one fresh physical mock loop, not live speech, calibration or statistical reliability acceptance. The interrupted adjustment-timeout session remains incomplete. Collector is finalized, mock stopped and tone finished; candidate firmware stays flashed. 192 host tests and P4 build/flash pass. Current disposition: text adapter and two approved live summary probes are verified; speech, three operator-rated live loops, full mock fixture/timing acceptance and original G-0001 gates remain open. No earlier failure or threshold is replaced. ADR-0011/0012 remain bounded as written.


2026-09-22: **Collector closeout limitation.** `physical/serial/summary.json` finalizes as FAIL with `capture_errors: ["malformed instrumentation"]`, no listed integrity errors, and passing SD/radio/HTTP startup checks. Preserve that summary unchanged. It does not replace or invalidate the separately scoped byte/ingress/transcript result in `physical/assessment-1.json`; neither assessment establishes complete serial-log integrity or full acceptance. All four known trial process IDs are absent after shutdown.
