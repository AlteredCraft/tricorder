# G-0001.03. Preserve measurement audio while processing speech

Status: In progress
Goal: [G-0001 — a trustworthy live investigation](G-0001-trustworthy-live-investigation.md)
ADRs: None — architectural choices remain under investigation.
Date: 2026-09-20
Revision: Initial proposal; no hardware results.

## Proposal

### Hypothesis

A shared capture source can supply a trustworthy pre-enhancement measurement stream and a separately processed conversation stream without cross-mutation, hidden sample loss or ambiguity about playback contamination.

### Refuted if

Enabling speech processing changes the same captured measurement block's bytes, raw buffers are reused before consumption, physical microphone channels are misidentified, samples disappear without a counter, or derived measurement results cannot be reproduced from saved raw audio and settings.

### Build

1. Capture sequence-numbered PCM blocks with sample counters, physical-channel mapping, fixed gain, format, clipping flags and timestamps. Hash the ingress blocks before handing them to consumers.
2. Feed a separate speech-processing path; keep raw data immutable while referenced. Verify with deterministic PCM injection/replay before acoustic tests.
3. Compare device FFT results to an independent desktop reference using synthetic bin-centered tones, mixtures, silence and impulses. Record identical window, FFT normalization and sample-rate conventions.
4. Run an acoustic fixture with a stationary sound source, documented distance/orientation and fixed gain. Capture speech with a background tone, with/without speech processing and with/without device playback; repeat under .02 load.

### Out of scope

This does not certify absolute sound-pressure level, microphone frequency response or fault diagnosis. A calibrated reference instrument would be needed for those claims. Open-air A/B recordings are not expected to be byte-identical. Model speech understanding and conversational interruption belong to .04.

### How we know

Use the [shared evidence contract](G-0001.01-hardware-baseline.md#shared-run-evidence). Numeric limits are proposed acceptance targets, not verified performance.

1. Ingress: every saved raw block hash matches its ingress hash and sample counters remain continuous. Derived streams carry source ranges and processing configuration.
2. Deterministic replay: raw output is bit-identical with speech processing enabled/disabled. Saved data survives SD write/read checksum checks. No consumer retains a buffer after release.
3. DSP: proposed targets for non-clipped synthetic signals are dominant frequency within one FFT bin and normalized peak amplitude within 1% of the reference for a bin-centered tone; apply amplitude comparisons only above the documented noise/numerical floor. Store per-case error and failed fixtures.
4. Acoustic/load run: zero unaccounted raw-sample gaps, correct channel response and clipping counts, and logged speaker-active intervals. Report speech-path latency and background-tone attenuation without assuming a pass means calibrated measurement. Use a proposed 100 ms p95 local speech-processing budget. If speech and measurement cannot coexist within the load budget, record that constraint and test an explicit mode switch.

Cross-run comparison: join summaries by spec revision and workload, retain all runs including failures, and compare at least three repetitions before claiming stable behavior. Differences in hardware, gain, clock, driver, model, codec or workload remain explicit.

## Observed

2026-09-20: **Not built, design only.** No build steps or device tests have been performed for this spec. All proposed checks and all out-of-scope items are **Not tested**. There are no measured outcomes or accepted design changes. Future entries must name the build step, dated result and evidence path, and link an ADR if evidence changes the design.

2026-09-20: **Host preparation checks written before implementation.** Prepare independent direct-DFT references for deterministic 2048-sample, 48 kHz PCM fixtures: bin-centered tone, mixture, silence and centered impulse. Tests require explicit periodic-Hann/coherent-gain normalization, correct DC/Nyquist treatment and known analytic amplitudes. Device DSP, speech separation, acoustic/load and SD acceptance remain gated on hardware evidence; no prerequisite is waived.

2026-09-20: **Host reference prepared.** Five analytic checks and the full 41-test host suite pass. Four deterministic PCM/DFT fixtures are retained in `.local/runs/20260920-audio-reference-2/`; the 867.1875 Hz tone recovers 0.250000159 full-scale amplitude. Preserve the first run, whose impulse peak label was misleading; tied/flat spectra now report no unique dominant frequency. Device/speech/SD/acoustic comparisons remain inconclusive. No new architectural decision: this is desktop test preparation only.

2026-09-20: **DSP kernel checks written before implementation.** Compile the candidate fixed-workspace C++ FFT on the host and compare its full one-sided spectrum with the independent direct DFT, using identical quantized fixtures/window/scaling. Reject invalid dimensions/slot/aliasing, preserve source bytes, report clipping and avoid a dominant-frequency claim for silence/flat spectra. This prepares firmware code; it does not replace on-device timing, channel mapping or SD evidence.

2026-09-20: **Candidate FFT validated on host.** Fixed-workspace C++ spectra match the direct DFT across all four 2048-frame fixtures within `1e-5` full-scale absolute error; tone peak meets the 1% amplitude target on the host. Slot selection, clipping, invalid input, alias rejection and source immutability checks pass; full suite is 44 tests. P4 compilation succeeds, but the kernel is not yet exercised on-device or under load. Evidence: `.local/runs/20260920-spectrum-host-tests.log` and baseline `spectrum-build-1.log`. No architecture decision; hardware selection/performance remain open.

2026-09-20: **Device fixture checks before implementation.** Embed the same quantized PCM fixtures, run the candidate FFT on P4 and export each full spectrum through the checked capture receiver. Host comparison must verify fixture SHA-256, unchanged input hashes, format/window/scaling, every finite bin, peak frequency/amplitude and clipping counts. Retain per-fixture device compute times separately from serial export; repeat three times before timing claims. Reject missing, corrupt or duplicate cases. These synthetic checks do not establish speech separation, acquisition continuity, acoustic mapping or SD durability.

2026-09-20: **Device synthetic DSP passes.** `device-spectrum-1/device-spectrum.json` verifies all four fixtures across three repetitions: complete spectra agree with the independent DFT (maximum absolute bin error `3.90e-7` full scale), peak targets pass, and input hashes remain unchanged. P4 computation takes 3.64–4.20 ms per fixture in this isolated run; serial/capture integrity passes. Fifty-six host tests pass. No new architecture decision: candidate arithmetic is verified, while speech separation, continuous acquisition, acoustic/load and SD gates remain open.
